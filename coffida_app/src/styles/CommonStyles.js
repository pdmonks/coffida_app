import { StyleSheet } from 'react-native';

const commonStyles = StyleSheet.create({
  background: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fffafa80', // snow transparent
    // backgroundColor: '#c0c0c080', // silver transparent
  },
});

export { commonStyles };
